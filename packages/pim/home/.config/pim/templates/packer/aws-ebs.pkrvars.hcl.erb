# Auto-generated by pim packer
# Build: <%= _build_name %>
# Generated: <%= _generated_at %>

ami_name      = "<%= vm_name || _build_name %>"
ami_description = "<%= ami_description || "Built by pim packer - #{_build_name}" %>"
source_ami    = "<%= source_ami %>"
region        = "<%= aws_region || 'us-east-1' %>"
instance_type = "<%= aws_instance_type || 't3.micro' %>"

ssh_username  = "<%= ssh_username || username || 'admin' %>"
ssh_timeout   = "<%= ssh_timeout || '10m' %>"

volume_size   = <%= aws_volume_size || disk_size&.to_i || 20 %>
volume_type   = "<%= aws_volume_type || 'gp3' %>"

tags = {
  Profile    = "<%= _profile_name %>"
  ISO_Base   = "<%= _iso_key || 'N/A' %>"
<% if defined?(hostname) && hostname %>
  Hostname   = "<%= hostname %>"
<% end %>
}
